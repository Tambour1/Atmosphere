services:
  php:
    build : .
    container_name: atmosphere-php
    volumes:
      - .:/var/www/html
    ports:
      - "3007:80"
    environment:
      - ENV_FILE=/var/www/html/.env
    env_file:
      - .env
